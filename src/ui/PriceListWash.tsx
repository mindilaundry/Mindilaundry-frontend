import "react-alice-carousel/lib/alice-carousel.css";

const PriceListWash = () => {
  return (
    <section className="flex h-max w-full flex-col items-center justify-between"></section>
  );
};

export default PriceListWash;
